# Module 3 - Kernel Module

Kernel module `hello_module` tao entry `/proc/hello_info`.

## Build va test

```bash
make
sudo ./test_module.sh
```

Script se build, load, doc `/proc/hello_info`, xem `dmesg`, roi unload module.
